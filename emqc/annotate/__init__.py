"""Slice annotation: overlay EM + segmentation, pick a label, fill / paint regions, scroll through z.
Blocks are directories holding em.npy (+ seg.npy) in (x, y, z) order, as produced by the H01 delivery."""
